services:
  - type: web
    name: orquestrador
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn orquestrador:app --host 0.0.0.0 --port 10000

  - type: web
    name: agente-pessoal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agente_pessoal:app --host 0.0.0.0 --port 10001

  - type: web
    name: agente-matematica
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agente_matematica:app --host 0.0.0.0 --port 10002
